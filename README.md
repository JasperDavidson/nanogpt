This repository serves as my playground for understanding GPT-style architectures, naive implementations of concepts, and doing basic performance experiments.

Currently implemented is a transformer-based LLM with basic KV caching.

QOL includes:
- Efficient hyperparameter search for training
- Parameter checkpointing for inference-time profiling
